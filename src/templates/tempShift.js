import React, { Component } from "react";
import {Shift} from 'ambient-cbg';

class TempShift extends Component {

    render() {
        return (
            <Shift />
        )
    }
}
export default TempShift;
